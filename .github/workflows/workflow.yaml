on:
  push:
    branches:
      - master
jobs:
  workflow:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: read Cache
        uses: actions/cache@v4
        with:
          path: /tmp/github-cache
          key: ${{ runner.os }}-github
          restore-keys: |
            ${{ runner.os }}-github

      - run: ls -lah /tmp/github-cache || true

      - run: bash .github/workflows/deploy_frontend.sh '${{ secrets.SA_KEY }}'
      - run: bash .github/workflows/deploy_backend.sh '${{ secrets.BACKEND_SA_KEY }}' '${{ secrets.SECRETS_JSON }}'

      - name: write Cache
        uses: actions/cache@v4
        with:
          path: /tmp/github-cache
          key: ${{ runner.os }}-github-${{ github.sha }}

      - run: ls -lah /tmp/github-cache || true
